# 🚀 Инструкция по деплою

Этот проект настроен для автоматического деплоя через GitHub Actions с использованием PM2.

## 📋 Предварительные требования

### На сервере должно быть установлено:
- Node.js (версия 18 или выше)
- PM2 (`npm install -g pm2`)
- Git
- Права на SSH доступ

## 🔧 Первоначальная настройка

### 1. Настройка сервера

Подключитесь к серверу и выполните:

```bash
# Создайте директорию для проекта
sudo mkdir -p /var/www/spoketa
sudo chown -R $USER:$USER /var/www/spoketa

# Клонируйте репозиторий
cd /var/www/spoketa
git clone <ваш-репозиторий> client
cd client

# Сделайте скрипт деплоя исполняемым
chmod +x deploy.sh

# Запустите первоначальную настройку
./deploy.sh
```

### 2. Настройка GitHub Secrets

Перейдите в Settings → Secrets and variables → Actions вашего репозитория и добавьте:

- `SERVER_HOST` - IP адрес или домен вашего сервера (например: `123.45.67.89`)
- `SERVER_USER` - пользователь для SSH (например: `ubuntu` или `root`)
- `SSH_PRIVATE_KEY` - приватный SSH ключ для доступа к серверу
- `SERVER_PORT` - (опционально) порт SSH, по умолчанию 22

#### Как получить SSH ключ:

На вашем локальном компьютере или сервере:

```bash
# Генерация новой SSH пары ключей (если у вас нет)
ssh-keygen -t ed25519 -C "github-actions"

# Скопируйте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/id_ed25519.pub user@your-server

# Скопируйте приватный ключ (весь вывод команды)
cat ~/.ssh/id_ed25519
```

Скопируйте весь вывод (включая `-----BEGIN` и `-----END`) в GitHub Secret `SSH_PRIVATE_KEY`.

### 3. Настройка файла .env на сервере

Создайте файл `.env` на сервере в директории проекта:

```bash
cd /var/www/spoketa/client
nano .env
```

Добавьте необходимые переменные окружения (например, API ключи).

## 🔄 Автоматический деплой

После настройки каждый push в ветку `main` будет автоматически:

1. ✅ Проверять код
2. 📦 Устанавливать зависимости
3. 🔨 Собирать проект
4. 🚀 Деплоить на сервер
5. ♻️ Перезапускать PM2

## 🛠 Полезные команды PM2

### На сервере:

```bash
# Проверить статус приложения
pm2 status

# Посмотреть логи
pm2 logs spoketa-client

# Перезапустить приложение
pm2 reload spoketa-client

# Остановить приложение
pm2 stop spoketa-client

# Запустить приложение
pm2 start ecosystem.config.js

# Мониторинг в реальном времени
pm2 monit

# Показать детальную информацию
pm2 show spoketa-client
```

## 📝 Структура файлов

- [.github/workflows/deploy.yml](.github/workflows/deploy.yml) - конфигурация GitHub Actions
- [ecosystem.config.js](ecosystem.config.js) - конфигурация PM2
- [deploy.sh](deploy.sh) - скрипт для первоначальной настройки

## 🔍 Отладка

Если деплой не работает:

1. Проверьте GitHub Actions logs в разделе Actions вашего репозитория
2. Проверьте логи PM2 на сервере: `pm2 logs spoketa-client`
3. Убедитесь, что все GitHub Secrets установлены правильно
4. Проверьте SSH соединение: `ssh -i ~/.ssh/id_ed25519 user@your-server`

## 🔐 Безопасность

- ❌ Никогда не коммитьте `.env` файлы
- ✅ Используйте GitHub Secrets для приватных данных
- ✅ Регулярно обновляйте зависимости
- ✅ Используйте SSH ключи вместо паролей

## 📊 Мониторинг

Рекомендуется настроить мониторинг через:
- PM2 Plus (https://pm2.io)
- CloudWatch (AWS)
- Или другие сервисы мониторинга

## 🆘 Поддержка

При возникновении проблем проверьте:
1. Логи GitHub Actions
2. Логи PM2: `pm2 logs`
3. Системные логи: `journalctl -u pm2-$USER`